<resources>


    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!--应用的主要色调,actionBar默认使用该颜色,Toolbar导航栏的底色-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--CheckBox,RadioButton,SwitchCompat等一般控件的选中效果默认采用该颜色-->
        <item name="colorAccent">@color/colorAccent</item>
        <!--默认window窗口(activity)背景颜色,根据主题修改该属性可 避免某些情况activity跳转/回跳会白屏(出现与主题相反的颜色)-->
        <item name="android:windowBackground">@color/windowBackground</item>
        <!-- B站风格:状态栏/导航栏与页面同色 -->
        <item name="android:statusBarColor">@color/bili_bg_card</item>
        <item name="android:navigationBarColor">@color/bili_bg_card</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:fitsSystemWindows">false</item>
        <!--
            横屏时,系统默认会按 display cutout(挖孔)letterbox 居中显示 Activity 内容,
            导致 4 个底部 tab 实际有效宽度 < 屏幕宽,视觉上左大右小/右大左小不对称。
            shortEdges 强制让窗口延伸到刘海/挖孔背后,Activity 内容区域 = 屏幕物理宽度。
        -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- 状态栏图标明暗由 BaseActivity 的 ImmersionBar.statusBarDarkFont 按主题动态设置 -->
        <item name="colorControlNormal">@color/bili_text_secondary</item>
    </style>

    <!--
        搜索页专用:横屏时前置摄像头在"长边"(左侧),
        shortEdges 模式只延伸到"短边"(顶/底)的挖孔,所以横屏下窗口不会铺到左侧挖孔,留下黑边。
        必须用 always,允许窗口延伸到任意位置的挖孔,才能真正铺满横屏。
        各区域(顶栏/热门搜索/结果页)已通过 OnApplyWindowInsetsListener 动态加挖孔内边距避免内容被摄像头遮挡。
    -->
    <style name="AppTheme.Search" parent="AppTheme">
        <item name="android:windowLayoutInDisplayCutoutMode">always</item>
    </style>

    <!-- 底部导航文字:选中 -->
    <style name="BiliNavTextActive" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">10sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- 底部导航文字:未选中 -->
    <style name="BiliNavTextInactive" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">10sp</item>
    </style>

    <!-- B站搜索胶囊:32dp高 16dp圆角 -->
    <style name="BiliSearchBar">
        <item name="android:layout_height">@dimen/bili_search_h</item>
        <item name="android:background">@drawable/bg_large_round_gray</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingHorizontal">12dp</item>
        <item name="android:textColor">@color/bili_text_primary</item>
        <item name="android:textColorHint">@color/bili_text_tertiary</item>
        <item name="android:textSize">13sp</item>
        <item name="android:singleLine">true</item>
    </style>

    <!-- B站卡片标题 -->
    <style name="BiliCardTitle">
        <item name="android:textColor">@color/bili_text_primary</item>
        <item name="android:textSize">14sp</item>
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
    </style>

    <!-- B站次级说明文字 -->
    <style name="BiliCardSub">
        <item name="android:textColor">@color/bili_text_tertiary</item>
        <item name="android:textSize">12sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <!-- B站分组标题 -->
    <style name="BiliSectionTitle">
        <item name="android:textColor">@color/bili_text_primary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- B站列表项容器:56dp高 白底 带右箭头 -->
    <style name="BiliListItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/bili_item_h</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingHorizontal">16dp</item>
        <item name="android:foreground">?android:selectableItemBackground</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:statusBarColor">@color/bili_bg_card</item>
        <item name="android:navigationBarColor">@color/bili_bg_card</item>
    </style>

    <!--
        启动主题:历史上给 launcher Activity(MainActivity) 专用,仅用于启动瞬间背景。
        现状(v1.0.14 起):真正的 launcher 已改为 SplashActivity(见 AndroidManifest.xml 的
        MAIN+LAUNCHER),SplashActivity 自身用 SplashTheme 承载完整的启动页(弹幕+跳过按钮+5秒)。
        MainActivity 现已改用 AppTheme,本 AppTheme.Launcher 仅作保留以防设备兼容回退,不再绑定启动入口。
        API 31+ 的覆盖见 values-v31/styles.xml(使用 framework 的 windowSplashScreen* 属性)。
        
        ★ 关键修复:之前 navigationBarColor=transparent + windowDrawsSystemBarBackgrounds=false
        导致从 Launcher 主题切换到 AppTheme 时,系统需要重新创建系统栏窗口,产生闪烁
        (深灰→黑→深灰)。现在改成和 AppTheme 一致的 bili_bg_card,切换时不会闪烁。
    -->
    <style name="AppTheme.Launcher" parent="AppTheme">
        <item name="android:windowBackground">@drawable/bg_splash</item>
        <item name="android:windowFullscreen">true</item>
        <!-- 删掉 windowDrawsSystemBarBackgrounds=false,用默认值 true -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:statusBarColor">@color/bili_bg_card</item>
        <item name="android:navigationBarColor">@color/bili_bg_card</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>//全屏即无通知栏
        <item name="android:windowContentOverlay">@null</item>//是否有遮盖
        <item name="windowNoTitle">true</item>
        <item name="android:keepScreenOn">true</item>
        <!--        <item name="android:windowBackground">@drawable/main_bg</item>-->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <!-- 自定义dialog样式 -->
    <style name="CustomDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="CustomDialogStyleDim" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="epg_window_btn">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/item_bg_selector_right</item>
        <item name="android:paddingLeft">0dip</item>
        <item name="android:paddingRight">0dip</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:clickable">true</item>
        <item name="android:drawablePadding">10.0dip</item>
        <item name="android:radius">10dp</item>
    </style>

    <style name="BottomDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_out_bottom</item>
    </style>

    <style name="DialogFadeAnimation">
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
    </style>

    <style name="TabVerticalItemStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">20dp</item>
        <item name="android:paddingBottom">20dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">16dp</item>
    </style>


    <!-- 实心通用主题色,MaterialButton样式-->
    <style name="mbSolid" parent="@style/Widget.MaterialComponents.Button.UnelevatedButton">

        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:gravity">center</item>
        <item name="backgroundTintMode">src_atop</item>
        <item name="backgroundTint">@color/colorPrimary</item>
        <!--        <item name="rippleColor">@color/white</item>-->

        <item name="iconTintMode">src_atop</item>
        <item name="iconTint">@color/white</item>
        <item name="iconGravity">textStart</item>
        <item name="iconPadding">0dp</item>
        <!--        <item name="iconSize">20dp</item>-->

        <item name="cornerRadius">@dimen/common_corners</item>
    </style>

    <!-- 带边框的空心MaterialButton样式-->
    <style name="mbOutline" parent="@style/Widget.MaterialComponents.Button.OutlinedButton">
        <!--        <item name="android:textColor">@color/colorPrimary</item>-->
        <!--        <item name="android:textAllCaps">false</item>-->
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">13sp</item>
        <!--        <item name="rippleColor">@color/white</item>-->
        <!--        <item name="strokeWidth">1dp</item>-->
        <item name="strokeColor">@color/colorPrimary</item>
        <item name="cornerRadius">@dimen/common_corners</item>
        <!--        <item name="iconTintMode">src_atop</item>-->
        <!--        <item name="iconTint">@color/white</item>-->
        <!--        <item name="iconGravity">textStart</item>-->
        <!--        <item name="iconPadding">0dp</item>-->
        <!--        <item name="iconSize">20dp</item>-->

    </style>

<!--文件选择器样式,在此只改变了背景和基本颜色-->
    <style name="FileChooser" parent="FileChooserStyle">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorPrimary</item>
        <item name="android:textColorSecondary">@color/colorPrimary</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/material_deep_teal_200</item>
        <item name="fileChooserDialogStyle">@style/FileChooserDialogStyle1</item>
        <item name="fileChooserListItemStyle">@style/FileChooserListItemStyle1</item>
        <item name="fileChooserPathViewStyle">@style/FileChooserPathViewStyle1</item>
    </style>

<!--选择器弹窗样式-->
    <style name="FileChooserDialogStyle1" parent="FileChooserDialogStyle">
        <item name="android:windowBackground">@drawable/bg_large_round_popup</item>
        <item name="android:buttonBarButtonStyle">@style/FileChooserButtonStyle1</item>
        <item name="buttonBarButtonStyle">@style/FileChooserButtonStyle1</item>
    </style>
    <style name="FileChooserButtonStyle1" parent="FileChooserButtonStyle">
        <item name="android:textColor">@color/black</item>
    </style>
<!--    选择器弹窗样式结束-->

<!--文件选择器条目样式开始-->
    <style name="FileChooserListItemStyle1" parent="FileChooserListItemStyle">
        <item name="fileChooserTextNameStyle">@style/FileChooserTextNameStyle1</item>
        <item name="fileChooserTextSizeStyle">@style/FileChooserTextSizeStyle1</item>
        <item name="fileChooserTextDateStyle">@style/FileChooserTextDateStyle1</item>
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="FileChooserTextNameStyle1" parent="FileChooserTextNameStyle">
        <item name="android:textColor">@color/text_foreground</item>
    </style>
    <style name="FileChooserTextSizeStyle1" parent="FileChooserTextSizeStyle">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>
    <style name="FileChooserTextDateStyle1" parent="FileChooserTextDateStyle">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>
    <!-- 圆形头像:ShapeableImageView 的圆形裁切 overlay -->
    <style name="CircleImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- 文件选择器条目样式结束-->

    <!-- 选择器文件路径样式   -->
    <style name="FileChooserPathViewStyle1" parent="FileChooserPathViewStyle">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/text_foreground</item>
    </style>


</resources>
